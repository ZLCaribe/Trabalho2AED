#include "Airport.h"

Airport::Airport(string cod, string name, string city, string country, double longitude, double latitude) :
    cod(cod),name(name),country(country),longitude(longitude),latitude(latitude){

}
